[#ftl]
{
  "id": ${(std.id)!"null"}, "user": { "name": "${(std.user.name)!}" }, "span": { "name": "${(std.span.name)!}" }, "state": { "major": { "name": "${(std.state.major.name)!}" }, "department": { "name": "${(std.state.department.name)!}" }, "squad": { "name": "${(std.state.squad.name)!}" } }
}
